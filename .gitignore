node_modules/
dist/
.gradle/
.kotlin/
build/
*.log
.DS_Store
.vsklib/
# Registry binding artifacts are re-downloaded by binding-gen; the .vsklib
# records and regenerated .ts typing files are the committed source of truth.
packages/cli-native/registry/**/*.aar
packages/cli-native/registry/**/*.jar
.aider*
# Generated per-platform project output and signing material. The iOS project
# is regenerated by `vesk bundle ios`; the keystore is a local secret supplied
# via env:VESK_STORE_PASSWORD / env:VESK_KEY_PASSWORD (see veskconfig.ts).
test-app/ios/
test-app/upload-keystore.jks

# Generated by scripts/sync-assets.mjs from committed sources (template/, Router.kt, sample .vsk).
packages/cli-native/assets/
